[package]
name = "hanteker_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
pretty_env_logger = "0.4"
anyhow = "1.0"

clap = { version = "3.1", features = ["derive", "suggestions", "wrap_help"] }
clap_complete = "3.1"

libusb = "0.3"

hanteker_lib = { path = "../hanteker_lib" }

